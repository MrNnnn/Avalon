<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MenuInfoMapper">
	<!-- 查询菜单 -->
	<select id="findAllByRole" resultType ="Menu">
		SELECT
			id,
			title,
			url,
			parentId
		FROM
			Fate_Menu
	</select>
</mapper>